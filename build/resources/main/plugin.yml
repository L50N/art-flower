name: ArtFlower
version: '1.0-SNAPSHOT'
main: de.l50n.artflower.main.ArtFlower
api-version: '1.20'
author: "L5ONdev"

commands:
  interface:
    description: "Use the command to open the ArtFlower interface. Then you can then create a flower within the UI."
    permission: "artflower.use"
    permission-message: "§8[§cArtFlower§8] §7You do not have the required authorizations! §o(artflower.use)"
    aliases:
      - af
      - artflower